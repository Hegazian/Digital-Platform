const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const user = await prisma.user.findFirst(); console.log('Read works'); await prisma.user.update({ where: { id: user.id }, data: { name: user.name + '1' }}); console.log('Write works'); } catch(e) { console.error(e); } finally { await prisma.$disconnect(); } } main();
